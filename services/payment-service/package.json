{
  "name": "payment-service",
  "version": "1.0.0",
  "description": "Settlr Payment Service — atomic transfers, idempotency, double-entry ledger. The most critical service.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.5",
    "@settlr/types": "*",
    "@settlr/config": "*",
    "@settlr/logger": "*",
    "@settlr/database": "*",
    "@settlr/redis": "*",
    "@settlr/kafka": "*"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
