{
  "name": "account-service",
  "version": "1.0.0",
  "description": "Settlr Account Service — account creation, balance queries, transaction history, auth.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.18.2",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "@settlr/types": "*",
    "@settlr/config": "*",
    "@settlr/logger": "*",
    "@settlr/database": "*",
    "@settlr/redis": "*",
    "@settlr/kafka": "*"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/bcrypt": "^5.0.2",
    "@types/jsonwebtoken": "^9.0.5",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
