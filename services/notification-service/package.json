{
  "name": "notification-service",
  "version": "1.0.0",
  "description": "Settlr Notification Service — sends email alerts via Resend.com on payment events from Kafka.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.18.2",
    "kafkajs": "^2.2.4",
    "@settlr/config": "*",
    "@settlr/logger": "*",
    "@settlr/types": "*",
    "@settlr/kafka": "*"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
